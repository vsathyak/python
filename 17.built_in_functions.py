#abs(number) --> absolute value
print("Absolute value of -10 is: ", abs(10))

#bool --> checck number is boolean or not
print("Check number 0 is boolean or not: ", bool(0))

#dir -->list of the attributes and methods of any object
dir("hello")

#get help for attributes or methods
sent="hello"
help(sent.upper)

#eval/exec --> evaluates the passed string as a Python expression and returns the result.
sum = 5+3
eval("sum")

#convert data-type
a=1
str(a) # converting a to string
int(a) #Converting a to int